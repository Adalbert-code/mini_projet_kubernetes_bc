apiVersion: v1
kind: Service
metadata:
  name: service-red
  namespace: production
  labels:
    app: web
    color: red
spec:
  type: ClusterIP
  selector:
    app: web
    color: red
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
