apiVersion: v1
kind: Service
metadata:
  name: service-blue
  namespace: production
  labels:
    app: web
    color: blue
spec:
  type: ClusterIP
  selector:
    app: web
    color: blue
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
